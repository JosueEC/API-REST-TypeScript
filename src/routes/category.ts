import { Router } from 'express'

const router = Router()

// * http://localhost:3000/category
router.get('/')

// * http://localhost:3000/category
router.post('/')

// * http://localhost:3000/category
router.delete('/')

export { router }
